name: Generate Trophy

on:
  schedule:
    - cron: '0 0 * * 1' # Weekly on Monday
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate trophy SVG
        uses: Erik-Donath/github-profile-trophy@feature/generate-svg
        with:
          username: TylersHub
          token: ${{ secrets.GITHUB_TOKEN }}
          # CORRECTED INPUTS:
          file: 'trophy.svg'           # Changed from 'output_path'
          theme: 'alduin'
          margin-width: '10'           # Changed from 'margin-w'
          no-frame: 'false'
          # NOTE: 'title' input is NOT supported by this Action. 
          # It will generate all default trophies.

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add trophy.svg
          git diff --quiet && git diff --staged --quiet || (git commit -m "🏆 Update trophy [skip ci]" && git push)   
